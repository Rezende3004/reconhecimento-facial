[project]
name = "reconhecimento-facial-pro"
version = "1.0.0"
description = "Reconhecimento facial em Python com OpenCV"
requires-python = ">=3.10"
dependencies = [
    "opencv-contrib-python==4.10.0.84",
    "numpy==2.1.3"
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
